# Externalized parameters for Relationship confirmation from SAP S4HANA to SAP Subscription Billing
# Auto-generated from PI/PO channel data. Review before import.
# Lines with <FILL_*> values require manual completion.

Credential_Alias=<FILL_Credential_Alias>
Receiver_Host=SubscriptionBilling
Receiver_Path=# 1. Clear the stale venv state (or just open a brand-new terminal) deactivate 2>/dev/null; hash -r  # 2. Mint needs these to build a complete venv (the usual real blocker) sudo apt update sudo apt install -y python3-full python3-venv python3-pip  # 3. Remove the half-made venv and recreate it cd ~/PycharmProjects/cpi_migrator rm -rf venv python3 -m venv venv  # 4. PROVE it built before going further — this is the check that was failing ls venv/bin/activate && venv/bin/python --version  # 5. Activate and install INTO the venv (no --break-system-packages needed now) source venv/bin/activate which python          # must print …/cpi_migrator/venv/bin/python pip install -r requirements.txt  # 6. Run streamlit run workbench.py
Timeout_ms=300000
